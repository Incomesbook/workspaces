# workspaces (seed)

Created: 2026-05-03

This repository is a *seed* for a workspace: it intentionally excludes bulky runtime caches
(e.g. _LIVE, _COLLECTED_FS, workspaceStorage, browser caches, *.jsonl, etc.).

If you need to backup those, do it via archives outside Git or via a dedicated storage strategy.
